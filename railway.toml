[build]
builder = "RAILPACK"

[deploy]
startCommand = "python run.py --dashboard-only"
healthcheckPath = "/api/status"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
